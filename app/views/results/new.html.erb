<% if current_user %>
  <%= render "form" %>
<% else %>
  <h1>New Result for <%= @game.display_name %></h1>
  <%= (image_tag @game.logo, alt: @game.name, class: "mb-2 w-1/8") unless @game.logo.nil? %>
  <h3>Please login to save your score.</h3>
<% end %>
